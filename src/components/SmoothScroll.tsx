"use client";

import { ReactNode } from "react";

/** Native scroll — Lenis removed for responsive, immediate wheel/trackpad feel */
export default function SmoothScroll({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
